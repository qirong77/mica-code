---
import DocLayout from '../../layouts/DocLayout.astro';

const reasoningEvent = '{ type: "reasoning", part: { type: "reasoning", text } }';
const turnResponse = '{ turn: { id, status: "inProgress" } }';
---

<DocLayout title="Headless 协议" active="headless" description="mica run / mica compact / mica commit / mica app-server 等无 UI 调用方式">
  <h1>Headless 协议</h1>
  <p>Mica 可以通过 JSON 协议被桌面应用或自动化工具调用，无需终端 UI。所有 headless 模式都会注册 MCP、支持 session 落盘与 subagent 清理，生命周期与交互模式一致。</p>

  <h2>一次性执行：mica run</h2>
  <pre><code>mica run --format json [--thinking] [--no-save] [--session &lt;id&gt;] [--dir &lt;cwd&gt;] [--mcp-init-timeout-ms &lt;ms&gt;] "&lt;prompt&gt;"</code></pre>
  <ul>
    <li>默认输出 <code>step_start</code>、<code>text</code>、<code>tool_use</code>、<code>error</code>、<code>step_finish</code> 事件（Codex exec 兼容的 ThreadEvent JSONL 形状）。</li>
    <li>每次工具调用先发 <code>state.status: "pending"</code>，完成后以相同 <code>callID</code> 发 <code>completed</code> 与结果，消费端可原位更新运行状态。</li>
    <li>显式传 <code>--thinking</code> 时额外输出 <code>{reasoningEvent}</code>；不传时保持精简输出以兼容现有消费者。</li>
    <li>headless 模式也会注册 <code>TodoWrite</code>，便于结构化展示运行计划。</li>
    <li><code>--mcp-init-timeout-ms</code> 为每个 MCP server 的 connect + tools/list 设置总截止时间，健康 server 仍会并行完成。</li>
    <li><code>--no-save</code> 在整轮 turn 期间跳过 session 落盘，适合一次性后台任务（如右键 Commit），不产生垃圾会话文件。</li>
    <li>Responses 协议启用 reasoning effort 时会请求 <code>summary: "auto"</code>，让支持该能力的模型产生可流式展示的思考摘要。</li>
    <li>prompt 中支持 <code>[Image](路径)</code> 引用本地图片（解析为多模态 content block 后再发给模型）。</li>
  </ul>

  <h2>上下文压缩：mica compact</h2>
  <pre><code>mica compact --session &lt;id&gt; [--dir &lt;cwd&gt;] [--force]</code></pre>
  <p>复用交互式 <code>/compact</code> 的 <code>CompactionService</code>（模型摘要 + 最近轮次保留），把压缩后的 checkpoint 写回会话文件并输出一行 JSON：<code>ok</code>、<code>mode</code>、<code>strategy</code>、before/after token 估计、<code>savedRatio</code>，以及供消费方展示压缩后上下文占用的 <code>contextWindowSize</code> / <code>contextUsageRatio</code> 与 <code>summarizedCount</code> / <code>keptCount</code>。会话内容较少时返回 <code>code: "not_needed"</code>。</p>

  <h2>Git 提交：mica commit</h2>
  <pre><code>mica commit [--dir &lt;cwd&gt;]</code></pre>
  <p>与交互式 <code>/commit</code> 复用同一套确定性分析/提交逻辑：程序先收集 git 变化摘要，再向模型<strong>只发一次请求</strong>生成 commit message（不启用工具、无多轮循环），随后程序自己执行 <code>git add</code> / <code>commit</code> / <code>push</code>，最后输出单行 JSON（<code>ok</code>、<code>commitHash</code>、<code>subject</code>、<code>commitMessage</code>、<code>pushed</code>，失败时含 <code>code</code> / <code>error</code>）。</p>

  <h2>常驻会话进程：mica app-server</h2>
  <pre><code>mica app-server [--session &lt;id&gt;] [--dir &lt;cwd&gt;] [--model &lt;id&gt;] [--variant &lt;effort&gt;] [--role &lt;name&gt;]</code></pre>
  <p>每会话一个常驻进程，从 stdin 读 Codex v2 App Server 协议请求（<code>initialize</code> / <code>thread/start</code> / <code>turn/start</code> / <code>turn/steer</code> / <code>turn/interrupt</code>），向 stdout 写 v2 通知。连续对话跳过进程启动、session 重载和 MCP 重复初始化。</p>
  <ul>
    <li><code>turn/start</code> 立即返回 <code>{turnResponse}</code> 后异步执行。</li>
    <li><code>turn/steer</code>（带 <code>expectedTurnId</code>）把输入注入活跃 turn 的 after_iteration 队列，实现忙时 Shift+Tab 续聊。</li>
    <li><code>turn/interrupt</code> 中止当前 turn；tool 执行中会在当前迭代/工具结束的边界生效。</li>
    <li>通知事件包括 <code>turn/started</code>、<code>turn/completed</code>、<code>item/agentMessage</code>、<code>item/reasoning</code>、<code>item/commandExecution</code>、<code>thread/tokenUsage/updated</code> 等。</li>
    <li>容错：<code>--session</code> resume 失败或 <code>--dir</code> chdir 失败时降级继续并发出携带真实原因的 error 通知，不退出进程。</li>
  </ul>
</DocLayout>
