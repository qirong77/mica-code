---
import DocLayout from '../../layouts/DocLayout.astro';

const base = import.meta.env.BASE_URL;

const configExample = `{
  "providers": [
    {
      "id": "deepseek",
      "name": "DeepSeek",
      "api_base": "https://api.deepseek.com",
      "protocol": "openai_chat_completions",
      "get_model_url": "https://api.deepseek.com/models",
      "api_key": ""
    },
    {
      "id": "Kimi",
      "name": "Kimi",
      "protocol": "openai_chat_completions",
      "api_base": "https://api.moonshot.cn/v1",
      "models": ["kimi-k2.7"],
      "api_key": ""
    }
  ],
  "mcpServers": {
    "sequential-thinking": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sequential-thinking"]
    }
  }
}`;
---

<DocLayout title="快速开始" active="getting-started" description="安装 Mica Code 并配置模型，开始你的第一次对话">
  <h1>快速开始</h1>

  <h2>安装</h2>
  <p>从 GitHub Release 下载对应平台的二进制（install.sh 探测 os/arch 并校验 sha256）：</p>
  <pre><code>curl -fsSL https://github.com/qirong77/mica-code/releases/latest/download/install.sh | sh
mica</code></pre>
  <p>安装器把二进制放到 <code>~/.local/lib/mica</code>，并在 <code>~/.local/bin/mica</code> 写入薄 launcher。首次运行会自动创建本地配置文件 <code>~/.mica/config.json</code>；设置 <code>MICA_HOME</code> 时配置路径解析到 <code>$MICA_HOME/config.json</code>。</p>

  <h2>配置模型</h2>
  <p>在 mica 中运行 <code>/config</code> 可以打开配置页面，或直接编辑配置文件。典型的 provider 配置：</p>
  <pre><code>{configExample}</code></pre>

  <ul>
    <li><code>protocol</code> 可选：<code>openai_chat_completions</code> 或 <code>openai_responses</code>。不要根据 <code>api_base</code> 猜测协议，按 provider 官方支持选择。</li>
    <li>provider 配置了 <code>get_model_url</code>，模型列表会在运行时按 OpenAI <code>/models</code> 响应格式拉取；没有动态接口时用静态 <code>models</code> 数组。</li>
    <li>模型列表也可以通过 <code>/model</code> 命令随时切换，并自动保存「按当前目录记忆」的 provider / model / effort 偏好。</li>
    <li>当前 provider 缺少 <code>api_key</code> 只是 warning，UI 可以启动，但发送第一条消息前仍需要可用 key。</li>
  </ul>

  <h2>第一次对话</h2>
  <pre><code>$ mica
▸ 你好，帮我看看当前目录的代码结构</code></pre>
  <ul>
    <li>输入 <code>@</code> 可搜索当前工作区文件并引用；粘贴图片时也可以用 <code>[Image](路径)</code> 引用剪贴板图片。</li>
    <li><code>Shift+Tab</code> 在输入框中按列表顺序循环切换 role（agent 运行中会被拒绝，此时表示排队发送）。</li>
    <li>运行中再发送消息会自动排队：<code>after_iteration</code> 在当前工具迭代边界注入，<code>after_turn</code> 在当前 turn 结束后发送。</li>
  </ul>

  <h2>运行时切换</h2>
  <ul>
    <li><code>/model</code> 切换 provider 与模型；<code>/effort</code> 切换推理强度（none / low / medium / high / xhigh）；<code>/role</code> 切换系统提示词。</li>
    <li>自定义 role 文件放在 <code>~/.mica/role/</code>（或 <code>$MICA_HOME/role/</code>），只加载 <code>.md</code> 文件，文件名作为 role 名。</li>
    <li><code>/status</code> 随时查看当前 provider / model / effort / role 状态，<code>/context</code> 查看上下文占用。</li>
  </ul>

  <h2>下一步</h2>
  <ul>
    <li>想了解全部内置命令：见 <a href={`${base}docs/commands`}>内置命令</a></li>
    <li>接入桌面应用 / 自动化脚本：见 <a href={`${base}docs/headless`}>Headless 协议</a></li>
    <li>多机器远程续聊：见 <a href={`${base}docs/sync`}>Mica Sync</a></li>
  </ul>
</DocLayout>
